<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.gmi.nordborglab.browser</groupId>
    <artifactId>genophenbrowser</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <prerequisites>
        <maven>${mavenVersion}</maven>
    </prerequisites>

    <developers>
        <developer>
            <id>uemit.seren</id>
            <name>Ümit Seren</name>
            <email>uemit.seren@gmail.com</email>
            <roles>
                <role>Developer</role>
            </roles>
            <organization>http://www.gmi.oeaw.ac.at</organization>
        </developer>
    </developers>

    <properties>
        <java.version>1.8</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <mavenVersion>3.0</mavenVersion>
        <skipTests>true</skipTests>

        <guava.version>20.0</guava.version>
        <gwt.version>2.8.2</gwt.version>
        <universal-analytics.version>2.2</universal-analytics.version>
        <junit.version>4.12</junit.version>
        <mockito.version>2.0.52-beta</mockito.version>
        <hibernate-validator.version>5.1.2.Final</hibernate-validator.version>
        <javax.validation.version>1.1.0.Final</javax.validation.version>
        <slf4j.version>1.7.16</slf4j.version>
        <logback.version>1.1.5</logback.version>
        <jetty.version>9.4.41.v20210516</jetty.version>
        <errai.version>3.2.2.Final</errai.version>
        <javassist.version>3.20.0-GA</javassist.version>
        <google.autovalue>1.1</google.autovalue>

        <maven.surefire.plugin>2.12.4</maven.surefire.plugin>
        <!-- don't upgrade to 3.1 until this https://issues.apache.org/jira/browse/MCOMPILER-205 is fixed -->
        <!-- http://stackoverflow.com/questions/31891219/illegalstateexception-in-hibernate-metamodel-generation-with-maven-->
        <maven.compiler.plugin>3.8.1</maven.compiler.plugin>
        <maven.source.plugin>3.2.1</maven.source.plugin>
        <maven.jar.plugin>2.4</maven.jar.plugin>
        <maven.tomcat7.plugin>2.2</maven.tomcat7.plugin>
        <maven.gwt.plugin>1.0.0</maven.gwt.plugin>
        <maven.exec.plugin>1.6.0</maven.exec.plugin>
        <maven.apt.plugin>1.1.1</maven.apt.plugin>
        <maven.resource.plugin>3.1.0</maven.resource.plugin>
        <maven.war.plugin>3.2.3</maven.war.plugin>
    </properties>

    <repositories>
        <repository>
            <id>vbc.artifactory</id>
            <name>artifactory.imp.ac.at-releases</name>
            <url>https://artifactory.imp.ac.at:443/local-mvn</url>
            <releases>
              <enabled>true</enabled>
            </releases>
            <snapshots>
              <enabled>true</enabled>
            </snapshots>
       </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.google.auto.value</groupId>
                <artifactId>auto-value</artifactId>
                <version>${google.autovalue}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.arcbees.analytics</groupId>
                <artifactId>universal-analytics</artifactId>
                <version>${universal-analytics.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.gwt</groupId>
                <artifactId>gwt</artifactId>
                <version>${gwt.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>3.1.0</version>
            </dependency>
            <!-- required because Hibernate 4.3 only works with javassist > 3.16 and errai-bus includes 3.15 -->
            <dependency>
                <groupId>org.javassist</groupId>
                <artifactId>javassist</artifactId>
                <version>${javassist.version}</version>
            </dependency>
            <dependency>
                <groupId>org.jboss.errai</groupId>
                <artifactId>errai-bus</artifactId>
                <version>${errai.version}</version>
            </dependency>
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>

            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>net.ltgt.gwt.maven</groupId>
                <artifactId>gwt-maven-plugin</artifactId>
                <inherited>false</inherited>
                <configuration>
                  <launcherDir>${project.build.directory}/gwt/launcherDir</launcherDir>
                  <codeserverArgs>
                    <param>-bindAddress</param>
                    <!-- change this to hostname (i.e. gmi-lws12.imp.ac.at) for remote local development if source maps should work -->
                    <param>gmi-lws12.imp.ac.at</param>
                    <param>-style</param>
                    <param>PRETTY</param>
                    <param>-XmethodNameDisplayMode</param>
                    <param>ABBREVIATED</param>
                </codeserverArgs>
                </configuration>
              </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>${maven.war.plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven.resource.plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <compilerVersion>${java.version}</compilerVersion>
                        <!-- required because of https://github.com/mkarneim/pojobuilder/issues/71 -->
                        <!--<compilerArgument>-proc:none</compilerArgument>-->
                    </configuration>
                    <dependencies>
                    </dependencies>
                </plugin>
                <plugin>
                    <groupId>org.eclipse.jetty</groupId>
                    <artifactId>jetty-maven-plugin</artifactId>
                    <version>${jetty.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>org.eclipse.jetty</groupId>
                            <artifactId>jetty-servlets</artifactId>
                            <version>${jetty.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                        <dependency>
                            <groupId>org.eclipse.jetty</groupId>
                            <artifactId>jetty-proxy</artifactId>
                            <version>${jetty.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                        <dependency>
                            <groupId>org.slf4j</groupId>
                            <artifactId>jcl-over-slf4j</artifactId>
                            <version>${slf4j.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                        <dependency>
                            <groupId>org.slf4j</groupId>
                            <artifactId>slf4j-api</artifactId>
                            <version>${slf4j.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                        <dependency>
                            <groupId>org.slf4j</groupId>
                            <artifactId>jul-to-slf4j</artifactId>
                            <version>${slf4j.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                        <dependency>
                            <groupId>ch.qos.logback</groupId>
                            <artifactId>logback-classic</artifactId>
                            <version>${logback.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                        <dependency>
                            <groupId>ch.qos.logback</groupId>
                            <artifactId>logback-access</artifactId>
                            <version>${logback.version}</version>
                            <scope>runtime</scope>
                        </dependency>
                    </dependencies>
                </plugin>
                <plugin>
                    <groupId>net.ltgt.gwt.maven</groupId>
                    <artifactId>gwt-maven-plugin</artifactId>
                    <version>${maven.gwt.plugin}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <sourceLevel>${java.version}</sourceLevel>
                        <failOnError>true</failOnError>
                    </configuration>
                </plugin>
                <plugin>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven.source.plugin}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <phase>package</phase>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.tomcat.maven</groupId>
                    <artifactId>tomcat7-maven-plugin</artifactId>
                    <version>${maven.tomcat7.plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${maven.exec.plugin}</version>
                </plugin>
                <plugin>
                    <groupId>com.mysema.maven</groupId>
                    <artifactId>apt-maven-plugin</artifactId>
                    <version>${maven.apt.plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.plugin}</version>
                    <configuration>
                        <parallel>methods</parallel>
                        <threadCount>10</threadCount>
                        <!-- Show the test errors on console -->
                        <useFile>false</useFile>
                        <printSummary>false</printSummary>
                        <includes>
                            <include>**/*Tests.java</include>
                        </includes>
                        <excludes>
                            <exclude>**/Abstract*.java</exclude>
                        </excludes>
                        <skipTests>${skipTests}</skipTests>
                    </configuration>
                </plugin>
            </plugins>

        </pluginManagement>
    </build>
    <modules>
        <module>genophenbrowser-client</module>
        <module>genophenbrowser-shared</module>
        <module>genophenbrowser-server</module>
    </modules>
</project>
